#include <bits/stdc++.h>
using namespace std;
string convert(string s, int numRows)
{
    int l = s.size();
    string new_s = "";
    for (int i = 0; i < numRows; i++)
    {
        if (i == 0 || i == numRows - 1)
        {
            temp = i;
            while (temp < l)
            {
            }
        }
    }
}
int main()
{
    string s;
    int numOfRows;
    cin >> s >> numRows;
    cout << convert(s, numRows);
}